<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:id="@+id/terminal_root"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/terminal_background"
              android:orientation="vertical">

  <androidx.appcompat.widget.Toolbar
    android:id="@+id/terminal_toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:visibility="visible"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Dark"/>

  <!-- Real swipeable pager: one page per open session (terminal or X). Adjacent
       pages are laid out and live (offscreenPageLimit = 1) so a horizontal drag
       peeks the destination tab's real content and snaps back if not crossed. -->
  <androidx.viewpager2.widget.ViewPager2
    android:id="@+id/terminal_pager"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/terminal_background"/>

</LinearLayout>