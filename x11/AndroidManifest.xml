<?xml version="1.0" encoding="utf-8"?>
<!--
  Library manifest for the embedded Termux:X11 X server / GUI.

  This is NeoTerm's own (modified) copy of termux-x11's manifest: the Java/AIDL/
  native sources still come straight from the pinned third_party/termux-x11
  submodule, but the manifest is vendored here so we can drop the LAUNCHER
  intent-filter — NeoTerm itself is the launcher, and it opens MainActivity
  in-process. Component class names resolve against this module's namespace
  (com.termux.x11); they are merged into the single NeoTerm APK.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <activity
            android:name=".MainActivity"
            android:taskAffinity=".MainActivity"
            android:launchMode="singleInstance"
            android:supportsPictureInPicture="true"
            android:configChanges="fontScale|orientation|screenSize|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|screenLayout|touchscreen|uiMode|smallestScreenSize|density"
            android:resizeableActivity="true"
            android:windowSoftInputMode="stateHidden"
            android:exported="true">
            <!-- No MAIN/LAUNCHER: NeoTerm is the launcher and opens this in-process. -->
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:theme="@style/Theme.AppCompat.DayNight"
            android:excludeFromRecents="true"
            android:name=".LoriePreferences"
            android:taskAffinity=".LoriePreferences"
            android:supportsPictureInPicture="false"
            android:resizeableActivity="true"
            android:exported="false" />

        <meta-data android:name="com.samsung.android.multidisplay.keep_process_alive" android:value="false" />
        <meta-data android:name="android.allow_multiple_resumed_activities" android:value="true" />

        <service android:name=".utils.KeyInterceptor"
            android:exported="true"
            android:label="NeoTerm X11 KeyInterceptor"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver android:name=".LoriePreferences$Receiver" android:exported="true" tools:ignore="ExportedReceiver" android:enabled="true">
            <intent-filter>
                <action android:name="com.termux.x11.CHANGE_PREFERENCE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
